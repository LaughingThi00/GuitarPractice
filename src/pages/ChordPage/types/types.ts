

export enum ContentType {
  Custom = "Custom",
  HarmonyBased = "HarmonyBased",
  TonicBased = "TonicBased",
}
export enum ModeType {
  Single = "Single",
  Multiple = "Multiple",
}

export enum TonicType {
  Basic = "Basic",
  BasicPassing = "Basic & Passing",
  Full = "Full",
}
export enum HarmonyType {
  Basic = "Basic",
  Advanced = "Advanced",
}
